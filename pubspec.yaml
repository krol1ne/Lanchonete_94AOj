name: lanchonete
description: "Projeto da materia de Front-End 94AOj - Lanchonete"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.5.4


dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0
  dio: ^5.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mockito: ^5.4.4
  build_runner: ^2.4.8
  http_mock_adapter: ^0.6.1
  network_image_mock: ^2.1.1


flutter:

  uses-material-design: true

  assets:
    - assets/images/
    # - assets/fonts/




